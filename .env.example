# Copy this file to .env and adjust if needed. .env is gitignored — never commit real secrets.

PGHOST=localhost
PGPORT=5432
PGUSER=cohort_admin
PGPASSWORD=changeme_local_dev_only
PGDATABASE=cohort_omop

# Only needed once cohort/llm_draft.py is wired up to a real LLM call.
ANTHROPIC_API_KEY=
