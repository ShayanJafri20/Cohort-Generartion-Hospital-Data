# Copy this file to .env and adjust if needed. .env is gitignored — never commit real secrets.

PGHOST=127.0.0.1
PGPORT=5434
PGUSER=cohort_admin
PGPASSWORD=changeme_local_dev_only
PGDATABASE=cohort_omop

# Free tier via Google AI Studio (no billing account needed): https://aistudio.google.com/apikey
# Without this set, cohort/llm_draft.py falls back to example_spec.json so the
# rest of the pipeline still runs.
GEMINI_API_KEY=
